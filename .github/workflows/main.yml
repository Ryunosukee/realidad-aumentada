# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the gh-pages branch
  push:
    branches: [ gh-pages ]
  pull_request:
    branches: [ gh-pages ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: GitHub raw url 1
        uses: ActionCloud/github-raw-url@v1
        with:
          # A normal GitHub url
          github-url: https://github.com/Ryunosukee/realidad-aumentada/blob/gh-pages/Logo%20Estrada_2022_9.glb
      
      - name: GitHub raw url 2
        uses: ActionCloud/github-raw-url@v1
        with:
          # A normal GitHub url
          github-url: https://github.com/Ryunosukee/realidad-aumentada/blob/gh-pages/Logo%20Estrada_2022_9.glb
